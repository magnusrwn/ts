enum SeatChoice {
    WINDOW = 10,
    MIDDLE = 11,
    AISLE = 12,
    FOURTH = 13,
}
// hardcodes the values this thing can get
let seatAssigned = SeatChoice.MIDDLE


enum SeatChoice1 {
    WINDOW = 10,
    MIDDLE,
    AISLE,
    FOURTH ,
}
// Hover to see how it auto assigns the trailing numbers to the other vars...